import { Header } from "@/components/header";
import { Fingerprint, KeyRound, ShieldCheck, UsersRound } from "lucide-react";

export default function SecurityPage() {
  return <main><div className="hero-shell compact-shell"><Header /><section className="page-hero"><span className="eyebrow">SECURITY MODEL</span><h1>Private limits stay private.<br /><em>Authority stays human.</em></h1><p>AccordOS separates model reasoning, deterministic authorization, and final human approval into distinct controls.</p></section></div><section className="section security-layout"><article><ShieldCheck /><h2>Fail-closed validation</h2><p>Every candidate offer is untrusted until a pure deterministic function verifies each required parameter, numeric boundary, and exact term. Invalid or incomplete values cannot be transmitted.</p></article><article><KeyRound /><h2>Organization-bound encryption</h2><p>Constraint profiles use AES-256-GCM encryption with keys derived for one organization. Using another organization’s identity causes authenticated decryption to fail.</p></article><article><UsersRound /><h2>Dual human approval</h2><p>Convergence creates an agreed proposal, not a binding contract. One authorized person from each organization must approve the identical terms.</p></article><article><Fingerprint /><h2>Privacy-preserving proof</h2><p>A SHA-256 digest of the approved record can be placed in a Solana memo transaction. The public proof contains no commercial terms.</p></article><div className="security-note"><b>Honest MVP boundary</b><p>This demonstration does not replace legal review, electronic signature, identity assurance, or a production key-management service. Production use requires configured authentication, managed secrets, database isolation policies, monitoring, and counsel-approved terms.</p></div></section></main>;
}
